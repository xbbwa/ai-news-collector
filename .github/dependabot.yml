# Keeps the SHA-pinned actions in .github/workflows up to date (one PR per bump, weekly).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
